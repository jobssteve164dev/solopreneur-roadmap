# SoloMap Agent Action Doctrine

## 0. Project-Level Priority

- 本项目的默认交付标准是“完整闭环做到当前能力面的最好”，不是追求最小实现路径。
- 当用户要求“落地”“修复”“闭环”“一步到位”“完善数据逻辑”“减少试错成本”时，Agent 必须把任务理解为完整系统能力交付，而不是只做一个能跑的窄补丁。
- “完整闭环”不等于无边界扩张；它要求把当前目标涉及的真实链路一次打通，并保留后续迭代入口。

## 1. Complete Loop Before Minimal Path

执行本项目功能型任务时，默认交付链路包括：

- 用户动作入口：用户如何发起、继续、查看或纠偏。
- 运行时主路径：代码如何实际执行，并与现有状态流兼容。
- 数据闭环：写入、读取、索引、迁移兼容、失败状态和历史记录都能自洽。
- Agent 接手闭环：下一位 Agent 能知道上一轮做了什么、改了哪里、验证了什么、风险在哪里。
- 工具闭环：如果数据在 SQLite、JSON、缓存或运行目录里，必须提供 Agent 可调用的脚本或明确入口。
- Prompt / skill 闭环：如果能力依赖 Agent 使用，必须把使用规则注入到任务提示或配套 skill 中。
- 文档闭环：长期架构、方法论或产品边界发生变化时，更新对应正式文档，不写低语义总结文件。
- 验证闭环：至少覆盖最窄真实链路；涉及运行时、脚本、序列化、prompt 注入或前台脚本时，验证最终生成物或真实调用路径。
- 发布闭环：运行时代码、资源、脚本或 package 变更推送后，检查仓库发布/CI workflow 结果。

## 2. Current-Capability Best Means

“做到当前能力面的最好”指：

- 优先利用仓库已有机制：`.solopreneur/` 数据、SQLite `execution_logs`、run digest、execution graph、prompt 注入、resources 工具、skills、测试和文档体系。
- 同一能力面不要只补一半。例如新增数据字段时，同时补读取、召回、工具查询、测试和文档；新增前台入口时，同时补状态、失败、刷新、历史和验证。
- 不把内部复杂度转嫁给用户。用户只需要看到动作、结果、风险和下一步，不需要理解 digest、graph、schema、SQLite 或索引细节。
- 后续迭代入口要真实存在：结构化数据、脚本命令、skill 规则、测试覆盖和长期文档至少留下一条可继续扩展的路径。

## 3. Guardrails

- 完整闭环不允许变成无关重构、重写架构或扩大产品边界；只围绕用户本轮目标涉及的链路补齐。
- 不为了“更稳”提前中止主路径；治理、超时、降级、缓存和保护只能服务结果达成。
- 不用跳过测试、禁用功能或只改文案制造完成感。
- 不把一次性执行流水、prompt、终端日志或内部过程复制进长期项目文档。
- 不做破坏性清理；需要删除非本轮新建文件时，先列清单并等待用户明确授权。

## 4. Required Working Pattern

1. 用一句话写清本轮要交付的完整闭环。
2. 先读真实代码、数据和相关长期文档，再动手。
3. 明确当前能力面缺口：入口、运行时、数据、工具、prompt/skill、文档、测试、发布。
4. 一次补齐直接相关链路，避免只做最小样例。
5. 运行能证明真实链路成立的验证。
6. 必要时更新项目记忆或长期文档。
7. 提交、推送，并检查 CI/发布结果。

## 5. Default Done Definition

本项目中，功能型任务只有同时满足以下条件才算完成：

- 用户目标对应的主路径可实际执行。
- 关键状态和数据可以被下一轮 Agent 或用户继续使用。
- 失败或未完成状态不会被误报成成功。
- 后续 Agent 能通过项目文件、工具或 skill 快速接手。
- 相关测试或真实命令已经验证。
- 变更已提交推送；如触发发布或 CI，结果已检查。
