{
  "name": "solomap-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "deploy": "set -a; . ../.env; set +a; wrangler deploy",
    "check": "set -a; . ../.env; set +a; wrangler deploy --dry-run",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "jose": "^6.2.8",
    "wrangler": "^4.0.0"
  },
  "overrides": {
    "sharp": "0.35.3",
    "undici": "7.29.0"
  }
}
