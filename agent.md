# Agent Execution Constraints

- 执行时不得把用户已经说清的目标语义降级成我熟悉的最小工程动作；必须按用户语义本身交付完整可验证结果。
- 任何增量修改必须先明确“现有不变量”和“本轮唯一增量”：用户未点名改变的目标、体验、路径、心智、边界、责任归属和既有行为均保持不变；内部实现边界不得推导出新的用户可见边界。实现若改变唯一增量之外的内容，视为未经授权的范围扩张，必须回退。
- 对本地优先产品，用户刚完成的选择、输入、展开、焦点和模式切换是当前权威状态；异步数据只能补充内容，不得重置或延迟这些动作。
- 任何缓存、渐进加载、后台校准或异步刷新都必须把用户可见状态视为“带源版本的派生投影”，不能把某个回调当时拿到的结果直接视为权威事实。读取开始时必须捕获源版本，提交或持久化前必须复核；源已变化就废弃旧结果并重新读取。完整快照与局部补丁必须使用明确且不同的更新契约，迟到结果不得覆盖更新状态，已知状态不得倒退或丢字段，所有快速首屏最终都必须收敛到当前真实状态。
- 聚合状态不得充当独立交互通道的占用锁；项目、节点或分组显示为运行中，只能表达汇总事实，不能据此冻结其他会话、输入框或动作入口。所有禁用、去重和并发判断都必须绑定到当前动作对应的稳定目标身份，并验证其他通道仍可独立输入和执行。
- 本地优先入口的响应边界从点击事件开始：面板创建、窗口 reveal、选中态和基础骨架必须先于 globalState、磁盘写入、数据库初始化、项目组合计算与扫描；不能只优化面板创建后的数据回包，却继续让入口点击被持久化或聚合工作阻塞。
- 涉及异步加载的 UI 修改，验收不得只证明缓存命中、调用顺序、请求仍为最新或单个函数正确；必须主动编排源在读取期间变化、旧请求晚到、局部补丁与完整快照交错等时序，并从最终用户界面断言状态不倒退、不丢失且最终收敛。
- 父子关系、会话归属等核心身份元数据不得依赖可选字段才保存；可选字段缺失时仍须保留关系，并优先使用稳定身份恢复历史归属。
- 任何由源码拼接、模板、生成器或序列化层产出后再交给 shell、Node、浏览器、插件宿主或外部 CLI 执行的脚本/命令，必须审计最终生成物而不是只看源代码意图；跨解释器边界要按最终运行语义验证引号、转义、路径和参数。
- SoloMap 创建或复用 VS Code Terminal 后，所有准备提示、环境命令和正式 Agent 命令必须统一经过终端进程就绪门闩并按终端串行投递；禁止在 `processId` 就绪前直接调用 `sendText`，也禁止用固定 sleep 猜测 Shell 已启动。任何新增命令窗口入口都必须复用该门闩，并由延迟 Shell 启动的专门回归测试与体验基线共同约束。
- 任何由 watcher、poller、回调、重试、文件哨兵或异步状态刷新触发的副作用动作，必须先保证同一业务对象的幂等性；重复观察同一状态不得重复创建终端、任务、复核、记录、发布或其他副作用。
- 权威状态完成异步终态写入时，用户可见状态刷新属于同一闭环事务：必须在写入成功后重新读取并提交对应作用域，写入前启动的缓存或 in-flight 读取必须失去提交权。关键回归测试必须主动让旧读取晚到，证明它不能把终态覆盖回进行中；只测持久化或展示函数均不算覆盖完整链路。
- 用户长期设置、偏好、授权、路径和工作环境配置都属于状态资产；任何局部设置动作只能补丁合并它明确修改的字段，缺失字段必须视为“本次未参与更新”，不得当成清空或默认值覆盖旧状态。
- 消息适配层不得把缺失字段重组为显式 `undefined` 后传给持久化层；`undefined` 必须继续保持“本次未参与更新”的语义，只有用户明确传入的空字符串、false 或具体枚举值才可表达清空、关闭或变更。
- 所有产出（如生成的图像、海报、临时资产等）必须保存在项目文件夹中（例如 `docs/assets/` 或项目内相应目录），决不能仅保存在 Gemini 内部文件夹（如 `<appDataDir>/brain/`），以确保所有数据完全归项目所有。
- SoloMap 已确认的对外命名、品牌标题、市场元信息、README 首屏标题和对应测试期望属于 P00 决策资产，除非用户本轮点名要求修改，否则任何功能任务不得触碰。
- 任何功能改动提交前必须逐项审计 diff 是否直接服务本轮目标，无法说明必要性的字段改动必须撤出。
