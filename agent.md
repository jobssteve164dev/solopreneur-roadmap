# Agent Execution Constraints

- 执行时不得把用户已经说清的目标语义降级成我熟悉的最小工程动作；必须按用户语义本身交付完整可验证结果。
- 对本地优先产品，用户刚完成的选择、输入、展开、焦点和模式切换是当前权威状态；异步数据只能补充内容，不得重置或延迟这些动作。
- 涉及异步加载的 UI 修改，验收必须主动重排事件顺序：先完成本地动作，再让缓存、节点、模型或外部数据依次返回，并断言用户状态不变。
- 父子关系、会话归属等核心身份元数据不得依赖可选字段才保存；可选字段缺失时仍须保留关系，并优先使用稳定身份恢复历史归属。
- 所有产出（如生成的图像、海报、临时资产等）必须保存在项目文件夹中（例如 `docs/assets/` 或项目内相应目录），决不能仅保存在 Gemini 内部文件夹（如 `<appDataDir>/brain/`），以确保所有数据完全归项目所有。
- 已确认的对外命名、品牌标题、插件/应用显示名和市场元信息属于产品决策；除非用户本轮明确要求改命名或营销表达，功能改动不得顺手修改这些字段、标题或测试期望。
